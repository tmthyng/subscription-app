<div class="mx-auto w-11/12 lg:w-1/2">
  <h1 class="mt-32 text-4xl font-bold uppercase">Subscriptions</h1>

  <button class='mt-3 px-4 py-2 text-sm font-semibold text-white bg-sky-400 hover:bg-sky-500 rounded-md'>New Subscription</button>

  <ul class='mt-12'>
    <% @pages.each do |page| %>
      <li>
        <%= page.title %> &rarr;
      </li>
    <% end %>
  </ul>
</div>
